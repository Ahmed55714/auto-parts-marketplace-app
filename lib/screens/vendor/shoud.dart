import 'package:flutter/material.dart';

class Shoud extends StatelessWidget {
  const Shoud({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Shoud'),);
  }
}
